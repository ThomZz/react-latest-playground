export default function About() {
  return (
    <section>
      <h1>About</h1>
      <p>
        This is the <code>/about</code> route — a second page wired up through
        React Router to show client-side navigation.
      </p>
    </section>
  );
}
